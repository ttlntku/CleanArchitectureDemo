name: Docker Image CI

on:
  push:
    branches: [ "task/test_workflows" ]
  pull_request:
    branches: [ "task/test_workflows" ]

jobs:

  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
    - name: Build the Docker image
      run: docker build . --file Dockerfile --tag kieudaike/cleanarchitecuredemo:latest
